# Entry Debugger 지원 기능 정리

확인일: 2026-05-24  
대상 버전: Entry Debugger `2.0.0`  
대상 확장 폴더: `entry-debugger-extension`

이 문서는 현재 확장 프로그램이 지원하는 기능을 제품 소개 웹페이지로 옮기기 쉽도록 정리한 기능 카탈로그입니다.

## 1. 한 줄 소개

Entry Debugger는 엔트리 만들기 화면에서 변수, 리스트, 신호, 장면, 함수 내부 사용 위치, 콘솔 로그, 실험 기능을 더 빠르게 확인하고 조작할 수 있게 해주는 디버깅 보조 확장 프로그램입니다.

## 2. 지원 범위

- 동작 URL: `https://playentry.org/ws/*`
- 권한: `storage`
- 실행 방식: Chrome Manifest V3 content script + page world 주입 스크립트
- 기본 목적: 엔트리 원본 코드를 수정하지 않고, 만들기 화면 안에서 디버깅과 탐색을 빠르게 하는 보조 도구
- 보안 방향: content script는 엔트리 공식 만들기 URL에서만 실행
- 값 수정, 리스트 편집, 스코프 변경, 오브젝트 추가처럼 사용자가 직접 누른 조작은 현재 프로젝트 상태에 반영됨

## 3. 기본 토글 구조

| 기능 | 위치 | 기본값 | 새로고침 필요 | 설명 |
|---|---|---:|---:|---|
| 모든 기능 | 확장 팝업 | 켜짐 | 일부 기능만 | 주요 기능을 한 번에 제어 |
| 디버깅 탭 | 확장 팝업 | 켜짐 | 보통 필요 | 엔트리 속성 패널에 `디버깅` 탭 추가 |
| 함수 사용 바로가기 | 확장 팝업 | 켜짐 | 보통 필요 | 속성 탭에 함수 내부 사용 위치 표시 |
| 콘솔 디버깅 | 확장 팝업 | 켜짐 | 보통 필요 | 말하기 블록에 외치기/로그 옵션 추가 |
| 함수 안에서 개인변수 보기 | 확장 팝업 | 켜짐 | 보통 필요 | 함수 편집 중 지역 변수/리스트 선택 가능 |
| 부스트 모드 | 확장 팝업 | 꺼짐 | 필요 | Entry WebGL 렌더링 옵션 활성화 |
| 실험실 탭 | 확장 팝업 | 꺼짐 | 보통 필요 | 실험 기능과 기본 변수 디버깅 표시 |

## 4. 디버깅 탭

엔트리 오른쪽 속성 패널에 기존 탭 디자인과 맞춘 `디버깅` 탭을 추가합니다. 탭 내부에서는 현재 프로젝트 상태를 실시간으로 읽어와 변수, 리스트, 신호, 장면을 관리합니다.

### 4.1 변수 탭

- 변수 이름과 현재 값을 실시간 표시
- 값 클릭 후 즉시 수정 가능
- 검색창으로 이름, 값, 스코프 기준 필터링
- 변수 스코프 표시:
  - `일반`
  - `공유`
  - `실시간`
  - `지역: 오브젝트이름`
- 변수 스코프 변경 가능
- 초시계/대답 같은 기본 변수는 일반 변수 목록에서 제외하고 실험실에서 별도 관리

### 4.2 리스트 탭

- 리스트 이름과 항목 개수 표시
- 리스트 펼치기/접기
- 항목 값 수정
- 항목 추가
- 항목 삭제
- 검색창으로 이름과 스코프 기준 필터링
- 리스트 스코프 표시 및 변경:
  - `일반`
  - `공유`
  - `실시간`
  - `지역: 오브젝트이름`

### 4.3 신호 탭

- 프로젝트에 등록된 신호 목록 표시
- 신호 이름 검색
- `신호 보내기` 버튼으로 선택한 신호 즉시 발생

### 4.4 장면 탭

- 프로젝트 장면 목록 표시
- 장면 이름 검색
- `이동` 버튼으로 선택한 장면으로 전환
- 작품 실행 중 장면 이동 시 `장면이 시작되었을 때` 이벤트를 함께 깨우도록 보조

### 4.5 실험실 탭

실험실 탭은 확장 팝업의 `실험실 탭` 토글이 켜져 있을 때만 보입니다. 기본값은 꺼짐입니다.

실험실에서 제공하는 기본 디버깅 대상:

- `초시계`
- `대답`

지원 기능:

- 현재 값 표시
- 값 직접 수정
- 표시 상태 확인
- 보이기/숨기기 버튼
- 숨기기 동작은 좌표를 `(500, 0)`으로 이동
- 보이기 동작은 좌표를 `(0, 0)`으로 이동

실험실 안의 추가 토글:

| 기능 | 기본값 | 설명 |
|---|---:|---|
| 터보 모드 | 꺼짐 | 엔트리 속도 조절 패널에 `∞` 단계 추가 |
| 다량 이미지 업로더 | 꺼짐 | 실험실 옆에 `업로더` 탭 표시 |
| 속성 검색으로 찾기 | 꺼짐 | 변수/신호/리스트 드롭다운에 검색 UI 추가 |

### 4.6 업로더 탭

`실험실 탭`과 `다량 이미지 업로더`가 모두 켜져 있을 때만 표시됩니다.

지원 기능:

- 여러 이미지 파일 선택
- 드래그 앤 드롭
- 오브젝트 이름 지정
- 선택한 이미지로 `.eo` 다운로드
- 선택한 이미지로 현재 엔트리 프로젝트에 오브젝트 바로 추가
- 선택 목록 비우기

지원 입력:

- PNG
- JPG/JPEG
- GIF
- WEBP
- SVG

변환 규칙:

- 비트맵 입력은 엔트리 호환을 위해 PNG로 변환
- SVG는 원본 SVG와 PNG 래스터 이미지, PNG 썸네일을 함께 생성
- BMP는 지원하지 않음
- `.eo` 다운로드 시 파일 확장자는 `.eo`로 저장

## 5. 함수 사용 바로가기

엔트리 기본 속성 탭은 변수, 리스트, 신호, 함수가 함수 내부에서 사용된 경우 `함수에 조립되어 있어요.` 정도로만 표시합니다. 이 기능은 기존 엔트리 표시 아래에 `함수에서 사용` 섹션을 추가해 실제 사용 위치를 더 자세히 보여줍니다.

지원 대상:

- 변수
- 리스트
- 신호
- 함수

표시 정보:

- 어느 함수에서 사용했는지
- 어떤 블록에서 사용했는지
- 클릭 가능한 바로가기

동작:

- 항목을 클릭하면 해당 함수 편집 화면으로 이동
- 가능한 경우 사용된 블록을 선택 상태로 표시
- 기존 엔트리 속성 패널과 별도 창 없이 통합 표시

아이디어 제공: `kkomaweb.com`

## 6. 콘솔 디버깅

말하기 계열 블록의 모드 드롭다운에 숨겨진 외치기 모드와 콘솔 로그용 옵션을 추가합니다.

대상 블록:

- `말하기`
- `N초간 말하기`

추가 옵션:

| 라벨 | 용도 |
|---|---|
| 외치기 | 엔트리에 이미 구현된 별 모양 말풍선 모드 표시 |
| `[LOG]` | 콘솔 기본 로그 |
| `[INFO]` | 콘솔 정보 로그 스타일 |
| `[WARN]` | 콘솔 경고 로그 스타일 |
| `[ERROR]` | 콘솔 오류 로그 스타일 |
| `[DEBUG]` | 콘솔 디버그 로그 스타일 |

특징:

- 기존 엔트리 `Dialog` 렌더링 기능을 활용
- 콘솔 로그 옵션은 말풍선을 띄우지 않고 콘솔에 출력
- 기존 프로젝트 JSON을 자동 변환하지 않음
- 확장 없이 열면 확장 전용 옵션은 엔트리 기본 UI에서 온전히 표시되지 않을 수 있음

아이디어 제공: `aqu3180.co.kr`

## 7. 함수 안에서 개인변수 보기

엔트리 함수 편집 화면에서는 원래 `이 오브젝트에서 사용` 변수와 리스트가 블록 꾸러미 드롭다운에 잘 보이지 않습니다. 이 기능은 함수 편집 중에도 현재 오브젝트의 개인 변수와 개인 리스트를 선택할 수 있게 합니다.

지원 대상:

- 변수 드롭다운
- 리스트 드롭다운
- 함수 편집 화면 안의 블록 꾸러미

특징:

- 프로젝트 JSON을 직접 수정하지 않음
- 드롭다운 목록 생성만 보조
- 만들어진 블록은 변수/리스트 ID를 참조하므로 확장 없이도 실행 가능
- 확장이 없으면 선택 UI 보조만 사라짐

## 8. 부스트 모드

엔트리의 WebGL 렌더링 옵션을 켜는 기능입니다.

특징:

- 내부적으로 `Entry.options.useWebGL`을 활성화
- Entry 초기화 시점에 반영되어야 하므로 새로고침 필요
- 확장 팝업에서 별도 토글
- 기본값은 꺼짐
- 무거운 그래픽 프로젝트에서 렌더링 성능 개선을 기대할 수 있음

## 9. 터보 모드

실험실 기능입니다. 엔트리 속도 조절 패널에 `∞` 단계를 추가합니다.

특징:

- 속도 조절 패널에 `1`, `15`, `30`, `45`, `60`, `∞` 숫자 표시
- `∞` 선택 시 FPS는 60으로 유지하고 `Entry.isTurbo`만 활성화
- 터보 모드가 켜질 때 속도 버튼이 깜빡여 상태 변화를 표시
- 작품 실행 중에도 다음 tick부터 반영 가능
- 기본값은 꺼짐

## 10. 속성 검색으로 찾기

실험실 기능입니다. 변수, 리스트, 신호 관련 블록의 드롭다운을 열었을 때 검색 입력창을 추가합니다.

지원 대상:

- 변수 드롭다운
- 리스트 드롭다운
- 신호 드롭다운

지원 조작:

- 키워드 검색
- 방향키 이동
- Enter 선택
- Escape 닫기

특징:

- 기존 블록 파라미터와 프로젝트 JSON을 변경하지 않음
- 드롭다운 UI만 검색 가능한 형태로 교체
- 기본값은 꺼짐

## 11. E.DEBUG 변수 콘솔 추적

디버깅 탭이 활성화되어 폴링 중일 때, 프로젝트에 `E.DEBUG`라는 이름의 변수가 있으면 값 변경을 브라우저 콘솔에 자동 출력합니다.

동작:

- `E.DEBUG` 변수를 감지하면 안내 로그 출력
- 값이 바뀔 때마다 `[E.DEBUG]` 접두사로 콘솔 출력
- 변수가 없으면 한 번 안내 메시지 출력
- 브라우저 콘솔 필터에 `[E.DEBUG]`를 입력해 관련 로그만 볼 수 있음

## 12. UI와 제품 톤

현재 UI 방향:

- 엔트리 기본 속성 패널 디자인에 맞춘 작은 탭 구조
- 확장 팝업은 기능별 토글 중심
- 실험 기능은 기본 비활성화
- 위험하거나 무거운 기능은 실험실 또는 별도 토글로 분리
- 기능 변경 안내 문구: `새로고침 해야 반영됩니다.`

## 13. 소개 웹페이지용 섹션 초안

웹페이지로 옮길 때 사용할 수 있는 추천 구조입니다.

1. Hero
   - 제목: `엔트리 디버깅을 더 빠르게`
   - 설명: `변수, 리스트, 신호, 장면, 함수 내부 사용 위치를 엔트리 화면 안에서 바로 확인하고 조작하세요.`

2. 핵심 기능
   - 디버깅 탭
   - 함수 사용 바로가기
   - 콘솔 디버깅
   - 함수 안에서 개인변수 보기

3. 실험실
   - 터보 모드
   - 속성 검색으로 찾기
   - 다량 이미지 업로더
   - 초시계/대답 디버깅

4. 안전한 동작 범위
   - `https://playentry.org/ws/*`에서만 실행
   - 필요한 권한은 `storage`
   - 실험 기능은 기본 꺼짐

5. 크레딧
   - 콘솔 디버깅 아이디어: `aqu3180.co.kr`
   - 함수 사용 바로가기 아이디어: `kkomaweb.com`

## 14. 기능별 구현 파일

| 기능 | 주요 파일 |
|---|---|
| 팝업 설정 | `popup.html`, `popup.js`, `settings.js`, `background.js` |
| 디버깅 탭 UI | `content.js`, `style.css` |
| Entry 데이터 읽기/쓰기 | `inject.js` |
| 함수 사용 바로가기 | `function-usage-inspector.js` |
| 콘솔 디버깅 | `console-debugging.js` |
| 함수 안 개인변수 보기 | `function-private-variables.js` |
| 부스트 모드 | `boost-mode.js` |
| 터보 모드 | `turbo-mode.js` |
| 속성 검색으로 찾기 | `dropdown-search.js` |
| 다량 이미지 업로더 | `eo-uploader.js` |
| 공통 page world 통신 | `page-bridge.js` |
| Entry 접근 어댑터 | `entry-adapter.js` |
| 중복 패치 방지 | `patch-registry.js` |

## 15. 주의 문구 후보

웹페이지나 스토어 설명에 함께 넣을 수 있는 문구입니다.

- 이 확장 프로그램은 엔트리 공식 서비스가 아닌 개인 개발 도구입니다.
- 일부 기능은 엔트리 내부 구조를 활용하므로 엔트리 업데이트에 따라 동작이 달라질 수 있습니다.
- 부스트 모드는 새로고침 후 반영됩니다.
- 실험실 기능은 기본적으로 꺼져 있으며, 필요한 경우에만 켜서 사용할 수 있습니다.
